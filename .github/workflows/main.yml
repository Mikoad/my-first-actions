name: My first actions

on:
  push:
    branches:
      - main

jobs:
  hello_job:
    runs-on: ubuntu-latest
    env:
      MY_VARIABLE: "This is a variable"
    steps:
      - name: Dire bonjour
        run: echo "${{env.MY_VARIABLE}}"
