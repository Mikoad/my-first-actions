name: My first actions

on:
  push:
    branches:
      - main

jobs:
  hello_job:
    runs-on: ubuntu-latest
    steps:
      - name: Dire bonjour
        run: echo "Hello, GitHub Actions!"
